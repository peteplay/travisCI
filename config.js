export const url = 'http://zero.webappsecurity.com/index.html'
export const login_username = 'username'
export const login_password = 'password'

// NB. the baseURL can also be set in the cypress.json file, but that will not allow to set a URL for each env
// add other urls for other test environments
// add other credentials for other users (admin/power user) with different permissions