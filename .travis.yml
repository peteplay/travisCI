language: node_js
node_js:
  - 'lts/*'
addons:
  apt:
    packages:
      # needed by Cypress, not in Ubuntu 16+ by default
      - libgconf-2-4
cache:
  npm: true
  directories:
    - ~/.npm
    - ./node_modules
    - ~/.cache
install:
  - npm ci
script:
  #- npm run cy:dashboard
  - $(npm bin)/cypress run --record
  #- npm run cy:ci
